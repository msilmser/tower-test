# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
        . ~/.bashrc
fi

# User specific environment and startup programs

PATH=$PATH:/users/mwe/jdk/bin
export PATH

export PS1="$(whoami)@($ZONE-$(hostname))"':$PWD $'

cd /users/mwe